# Bridge asset manifest

## Production base

- `base/bridge-master.png` — communications bridge visual background.

## Dynamic content (build later)

The communications terminal is a DOM/UI layer over the scene:

- six seeded mixed Chinese/English messages that drift like danmaku;
- a clearly labelled `COMING SOON` guest-message entry point;
- future message moderation and publication via the private admin system.

Do not rasterize message text into the room image. The map tablet and return control are likewise interactive HTML layers so navigation stays consistent across scenes.
